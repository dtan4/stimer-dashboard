<!DOCTYPE html>
<html>
  <head>
    <title>systemd timer dashboard</title>
    <meta charset="utf-8">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.3.1/css/bulma.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <h1 class="title">systemd timer dashboard</h1>
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Last Triggered</th>
            <th>Result</th>
            <th>Next</th>
          </tr>
        </thead>
        <tbody>
          {{ range .timers }}
          <tr>
            <td>{{ .UnitName }}</td>
            <td>{{ .LastTriggeredAt }}</td>
            <td><span class=" tag is-success">{{ .Result }}</span></td>
            <td>{{ .NextTriggerAt }}</td>
          </tr>
          {{ end }}
        </tbody>
      </table>
    </div>
  </body>
</html>
